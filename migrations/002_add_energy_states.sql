-- Migration 002: add_energy_states
-- No-op: pib_energy_states table was folded into 001_initial_schema.sql
-- This placeholder preserves the numbering sequence referenced in the build spec.

SELECT 1;

-- DOWN
SELECT 1;
